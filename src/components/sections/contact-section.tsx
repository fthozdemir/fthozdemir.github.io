import React from "react";

export const ContactSection = () => {
    return (
        <section className="page" data-page="contact">
            <div className="page_content contact">
                {/* Contact information, such as email links */}
            </div>
        </section>
    );
};
